# Task Breakdown

这是从各份规格文档抽出来的直接执行顺序，只保留可以直接实施的任务，不重复展开规格正文。

## Phase 1: 内存卡模板 V2

状态：已完成

1. 已完成：更新参数类型
2. 已完成：更新参数面板
3. 已完成：实现自动尺寸推荐搜索器
4. 已完成：重写内存卡模板建模逻辑
5. 已完成：增加推荐说明和失败态
6. 已完成：增加回归测试

## Phase 2: 钳类模板 V2

1. 更新钳类参数类型
2. 收敛核心字段
3. 实现渐缩导向槽几何
4. 接入自动尺寸推荐
5. 增加说明和失败态
6. 增加典型钳型测试

## Phase 3: 通用能力补强

1. 抽公共“自动推荐外部尺寸”模块
2. 抽公共“条件字段 + 高级设置”UI 结构
3. 增加更多模板的复用入口

## Phase 4: 参数化型腔盒模板 V1

状态：已完成

1. 已完成：拆出独立模板 `parametric-cavity-bin` 与 `ParametricCavityBinParams`
2. 已完成：收口 `generic-bin` 为纯隔板模板，不再混合型腔模式切换
3. 已完成：迁移基础形状自动排布、姿态求解、固定尺寸校验和独立型腔建模
4. 已完成：新增独立工作流，支持手动点击“生成图形”、复制 / 粘贴 / 粘贴新增和固定 `X / Y / Z` 旋转编辑
5. 已完成：移除自动旋转与姿态限制开关，新增 `x-first / y-first` 排列方式与摘要展示
6. 已完成：补齐回归测试，覆盖手动旋转、排列方式切换、独立型腔、固定尺寸失败和页面状态隔离

## Phase 5: 参数面板紧凑化

状态：已完成

1. 已完成：通用参数面板升级为 Unity Inspector 风格的 `X / Y / Z` 分组输入
2. 已完成：数字输入统一为手动输入，不再显示上下箭头
3. 已完成：参数说明收口到悬浮提示，字段标题尽量缩短并保持单行显示
4. 已完成：左侧控制栏加宽并收紧布局，减少标题换行与按钮挤压
5. 已完成：专用工作流与通用参数面板统一使用紧凑样式和颜色化坐标轴识别

## Phase 6: 参数列表界面 V2 与超宽屏适配

状态：已完成

1. 已完成：扩展 `src/lib/gridfinity/types.ts` 的参数字段展示元数据，支持 section、展示样式和布局提示，并保留默认回退行为
2. 已完成：更新 `src/lib/gridfinity/templates.ts` 中通用模板字段配置，补齐 `基础尺寸 / 内部结构 / 附加功能 / 高级设置` 等 section
3. 已完成：重构 `src/components/ParameterPanel.tsx`，从扁平 `form-grid` 升级为 `section -> block -> control`
4. 已完成：补齐卡片式单选、segmented control、整行 `switch-row`、成组数值输入等公共控件与样式
5. 已完成：调整 `src/pages/GeneratorPage.tsx` 与相关 workflow 的主操作区布局，兼容自动生成模板和参数化型腔盒双态流程
6. 已完成：在 `src/index.css` 增加 `>= 1600px` 与 `>= 1920px` 的超宽屏断点，重平衡控制栏、预览区和摘要区占比
7. 已完成：补齐页面与组件回归验证，覆盖条件字段、高级折叠、重置、错误态和多档宽度布局稳定性
